# API base URL for frontend requests (must include /api prefix)
# Dev with Vite proxy:
VITE_API_URL=/api
# Production (Render example — note the trailing /api):
# VITE_API_URL=https://mini-ecommerce-q8ba.onrender.com/api
